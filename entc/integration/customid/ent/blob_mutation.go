// Copyright 2019-2026 Facebook Inc.
// SPDX-License-Identifier: Apache-2.0

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"slices"

	"github.com/google/uuid"
	"github.com/neko-sc/ent"
	"github.com/neko-sc/ent/dialect/sql"
	"github.com/neko-sc/ent/entc/integration/customid/ent/blob"
)

// BlobInsert is the write representation of a Blob creation.
type BlobInsert struct {
	ID          ent.Option[uuid.UUID] `json:"id,omitzero"`
	UUID        ent.Option[uuid.UUID] `json:"uuid,omitzero"`
	Count       ent.Option[int]       `json:"count,omitzero"`
	ParentID    ent.Option[uuid.UUID] `json:"parent_id,omitzero"`
	LinkIDs     []uuid.UUID           `json:"link_ids,omitzero"`
	expressions map[string]func(*sql.Builder)
}

// BlobPatch is the write representation of a Blob update.
type BlobPatch struct {
	UUID         ent.Option[uuid.UUID]        `json:"uuid,omitzero"`
	Count        ent.Option[int]              `json:"count,omitzero"`
	CountAdd     ent.Option[int]              `json:"count_add,omitzero"`
	ParentID     ent.Option[uuid.UUID]        `json:"parent_id,omitzero"`
	Links        ent.RelationPatch[uuid.UUID] `json:"links,omitzero"`
	expressions  map[string]func(*sql.Builder)
	clearedEdges map[string]bool
}

func (i *BlobInsert) set(column string, value any) error {
	switch column {

	case blob.FieldID:
		v, ok := value.(uuid.UUID)
		if !ok {
			return &ValidationError{Name: column, err: fmt.Errorf("ent: unexpected type %T for field %q of Blob", value, column)}
		}
		i.ID = ent.Some(v)

		delete(i.expressions, column)
		return nil

	case blob.FieldUUID:
		v, ok := value.(uuid.UUID)
		if !ok {
			return &ValidationError{Name: column, err: fmt.Errorf("ent: unexpected type %T for field %q of Blob", value, column)}
		}
		i.UUID = ent.Some(v)

		delete(i.expressions, column)
		return nil

	case blob.FieldCount:
		v, ok := value.(int)
		if !ok {
			return &ValidationError{Name: column, err: fmt.Errorf("ent: unexpected type %T for field %q of Blob", value, column)}
		}
		i.Count = ent.Some(v)

		delete(i.expressions, column)
		return nil

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Blob is not settable", column)}
}

func (i *BlobInsert) setNull(column string) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Blob is not nullable", column)}
}

func (i *BlobInsert) setExpr(column string, render func(*sql.Builder)) {
	if i.expressions == nil {
		i.expressions = make(map[string]func(*sql.Builder))
	}
	i.expressions[column] = render
}

func (i *BlobInsert) setEdge(edge string, id any) error {
	switch edge {

	case blob.EdgeParent:
		value, ok := id.(uuid.UUID)
		if !ok {
			return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of Blob", id, edge)}
		}
		i.ParentID = ent.Some(value)
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Blob is not settable", edge)}
}

func (i *BlobInsert) addIDs(edge string, ids ...any) error {
	switch edge {

	case blob.EdgeLinks:
		for _, id := range ids {
			value, ok := id.(uuid.UUID)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of Blob", id, edge)}
			}
			i.LinkIDs = append(i.LinkIDs, value)
		}
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Blob does not accept IDs", edge)}
}

func (i *BlobInsert) parentIDs() []uuid.UUID {

	if id, ok := i.ParentID.Get(); ok {
		return []uuid.UUID{id}
	}
	return nil

}

func (i *BlobInsert) linksIDs() []uuid.UUID {
	return i.LinkIDs
}

func (i *BlobPatch) set(column string, value any) error {
	switch column {

	case blob.FieldUUID:
		v, ok := value.(uuid.UUID)
		if !ok {
			return &ValidationError{Name: column, err: fmt.Errorf("ent: unexpected type %T for field %q of Blob", value, column)}
		}
		i.UUID = ent.Some(v)

		delete(i.expressions, column)
		return nil

	case blob.FieldCount:
		v, ok := value.(int)
		if !ok {
			return &ValidationError{Name: column, err: fmt.Errorf("ent: unexpected type %T for field %q of Blob", value, column)}
		}
		i.Count = ent.Some(v)

		i.CountAdd = ent.Unset[int]()

		delete(i.expressions, column)
		return nil

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Blob is not settable", column)}
}

func (i *BlobPatch) setNull(column string) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Blob is not nullable", column)}
}

func (i *BlobPatch) setExpr(column string, render func(*sql.Builder)) {
	if i.expressions == nil {
		i.expressions = make(map[string]func(*sql.Builder))
	}
	i.expressions[column] = render
}

func (i *BlobPatch) setEdge(edge string, id any) error {
	switch edge {

	case blob.EdgeParent:
		value, ok := id.(uuid.UUID)
		if !ok {
			return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of Blob", id, edge)}
		}
		i.ParentID = ent.Some(value)
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Blob is not settable", edge)}
}

func (i *BlobPatch) addIDs(edge string, ids ...any) error {
	switch edge {

	case blob.EdgeLinks:
		for _, id := range ids {
			value, ok := id.(uuid.UUID)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of Blob", id, edge)}
			}
			i.Links.Add = append(i.Links.Add, value)
		}
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Blob does not accept IDs", edge)}
}

func (i *BlobPatch) parentIDs() []uuid.UUID {

	if id, ok := i.ParentID.Get(); ok {
		return []uuid.UUID{id}
	}
	return nil

}

func (i *BlobPatch) linksIDs() []uuid.UUID {
	return i.Links.Add

}

func (p *BlobPatch) add(column string, value any) error {
	switch column {

	case blob.FieldCount:
		delta, ok := value.(int)

		if !ok {
			return &ValidationError{Name: column, err: fmt.Errorf("ent: unexpected addition type %T for field %q", value, column)}
		}
		if current, exists := p.CountAdd.Get(); exists {
			delta += current
		}
		p.CountAdd = ent.Some(delta)
		return nil

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Blob does not support addition", column)}
}

func (p *BlobPatch) appendValues(column string, value any) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Blob does not support append", column)}
}

func (p *BlobPatch) removeIDs(edge string, ids ...any) error {
	switch edge {

	case blob.EdgeLinks:
		for _, id := range ids {
			value, ok := id.(uuid.UUID)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q", id, edge)}
			}
			p.Links.Remove = append(p.Links.Remove, value)
			p.Links.Add = slices.DeleteFunc(p.Links.Add, func(id uuid.UUID) bool { return id == value })
		}
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Blob does not support removal", edge)}
}

func (p *BlobPatch) clearEdge(edge string) error {
	switch edge {

	case blob.EdgeParent:

		if p.clearedEdges == nil {
			p.clearedEdges = make(map[string]bool)
		}
		p.clearedEdges[edge] = true
		p.ParentID = ent.Null[uuid.UUID]()
		return nil

	case blob.EdgeLinks:
		p.Links.Clear = true
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Blob cannot be cleared", edge)}
}

func (p *BlobPatch) apply(other BlobPatch) {

	if other.UUID.IsSet() {
		p.UUID = other.UUID
		delete(p.expressions, blob.FieldUUID)
	}

	if other.Count.IsSet() {
		p.Count = other.Count
		delete(p.expressions, blob.FieldCount)
	}

	if value, ok := other.CountAdd.Get(); ok {
		if current, exists := p.CountAdd.Get(); exists {
			value += current
		}
		p.CountAdd = ent.Some(value)
	}

	if other.ParentID.IsSet() {
		p.ParentID = other.ParentID
	}

	p.Links.Add = append(p.Links.Add, other.Links.Add...)
	p.Links.Remove = append(p.Links.Remove, other.Links.Remove...)
	p.Links.Clear = p.Links.Clear || other.Links.Clear

	for column, render := range other.expressions {
		p.setExpr(column, render)
	}
	for edge, clear := range other.clearedEdges {
		if clear {
			if p.clearedEdges == nil {
				p.clearedEdges = make(map[string]bool)
			}
			p.clearedEdges[edge] = true
		}
	}
}
