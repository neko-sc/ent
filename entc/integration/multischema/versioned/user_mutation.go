// Copyright 2019-2026 Facebook Inc.
// SPDX-License-Identifier: Apache-2.0

// Code generated by ent, DO NOT EDIT.

package versioned

import (
	"fmt"
	"slices"

	"github.com/neko-sc/ent"
	"github.com/neko-sc/ent/dialect/sql"
	"github.com/neko-sc/ent/entc/integration/multischema/versioned/user"
)

// UserInsert is the write representation of a User creation.
type UserInsert struct {
	Name          ent.Option[string] `json:"name,omitzero"`
	PetIDs        []int              `json:"pet_ids,omitzero"`
	GroupIDs      []int              `json:"group_ids,omitzero"`
	FriendIDs     []int              `json:"friend_ids,omitzero"`
	FollowerIDs   []int              `json:"follower_ids,omitzero"`
	FollowingIDs  []int              `json:"following_ids,omitzero"`
	FriendshipIDs []int              `json:"friendship_ids,omitzero"`
	expressions   map[string]func(*sql.Builder)
}

// UserPatch is the write representation of a User update.
type UserPatch struct {
	Name         ent.Option[string]     `json:"name,omitzero"`
	Pets         ent.RelationPatch[int] `json:"pets,omitzero"`
	Groups       ent.RelationPatch[int] `json:"groups,omitzero"`
	Friends      ent.RelationPatch[int] `json:"friends,omitzero"`
	Followers    ent.RelationPatch[int] `json:"followers,omitzero"`
	Following    ent.RelationPatch[int] `json:"following,omitzero"`
	Friendships  ent.RelationPatch[int] `json:"friendships,omitzero"`
	expressions  map[string]func(*sql.Builder)
	clearedEdges map[string]bool
}

func (i *UserInsert) set(column string, value any) error {
	switch column {

	case user.FieldName:
		v, ok := value.(string)
		if !ok {
			return &ValidationError{Name: column, err: fmt.Errorf("ent: unexpected type %T for field %q of User", value, column)}
		}
		i.Name = ent.Some(v)

		delete(i.expressions, column)
		return nil

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of User is not settable", column)}
}

func (i *UserInsert) setNull(column string) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of User is not nullable", column)}
}

func (i *UserInsert) setExpr(column string, render func(*sql.Builder)) {
	if i.expressions == nil {
		i.expressions = make(map[string]func(*sql.Builder))
	}
	i.expressions[column] = render
}

func (i *UserInsert) setEdge(edge string, id any) error {
	switch edge {

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of User is not settable", edge)}
}

func (i *UserInsert) addIDs(edge string, ids ...any) error {
	switch edge {

	case user.EdgePets:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of User", id, edge)}
			}
			i.PetIDs = append(i.PetIDs, value)
		}
		return nil

	case user.EdgeGroups:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of User", id, edge)}
			}
			i.GroupIDs = append(i.GroupIDs, value)
		}
		return nil

	case user.EdgeFriends:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of User", id, edge)}
			}
			i.FriendIDs = append(i.FriendIDs, value)
		}
		return nil

	case user.EdgeFollowers:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of User", id, edge)}
			}
			i.FollowerIDs = append(i.FollowerIDs, value)
		}
		return nil

	case user.EdgeFollowing:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of User", id, edge)}
			}
			i.FollowingIDs = append(i.FollowingIDs, value)
		}
		return nil

	case user.EdgeFriendships:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of User", id, edge)}
			}
			i.FriendshipIDs = append(i.FriendshipIDs, value)
		}
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of User does not accept IDs", edge)}
}

func (i *UserInsert) petsIDs() []int {
	return i.PetIDs
}

func (i *UserInsert) groupsIDs() []int {
	return i.GroupIDs
}

func (i *UserInsert) friendsIDs() []int {
	return i.FriendIDs
}

func (i *UserInsert) followersIDs() []int {
	return i.FollowerIDs
}

func (i *UserInsert) followingIDs() []int {
	return i.FollowingIDs
}

func (i *UserInsert) friendshipsIDs() []int {
	return i.FriendshipIDs
}

func (i *UserPatch) set(column string, value any) error {
	switch column {

	case user.FieldName:
		v, ok := value.(string)
		if !ok {
			return &ValidationError{Name: column, err: fmt.Errorf("ent: unexpected type %T for field %q of User", value, column)}
		}
		i.Name = ent.Some(v)

		delete(i.expressions, column)
		return nil

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of User is not settable", column)}
}

func (i *UserPatch) setNull(column string) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of User is not nullable", column)}
}

func (i *UserPatch) setExpr(column string, render func(*sql.Builder)) {
	if i.expressions == nil {
		i.expressions = make(map[string]func(*sql.Builder))
	}
	i.expressions[column] = render
}

func (i *UserPatch) setEdge(edge string, id any) error {
	switch edge {

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of User is not settable", edge)}
}

func (i *UserPatch) addIDs(edge string, ids ...any) error {
	switch edge {

	case user.EdgePets:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of User", id, edge)}
			}
			i.Pets.Add = append(i.Pets.Add, value)
		}
		return nil

	case user.EdgeGroups:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of User", id, edge)}
			}
			i.Groups.Add = append(i.Groups.Add, value)
		}
		return nil

	case user.EdgeFriends:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of User", id, edge)}
			}
			i.Friends.Add = append(i.Friends.Add, value)
		}
		return nil

	case user.EdgeFollowers:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of User", id, edge)}
			}
			i.Followers.Add = append(i.Followers.Add, value)
		}
		return nil

	case user.EdgeFollowing:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of User", id, edge)}
			}
			i.Following.Add = append(i.Following.Add, value)
		}
		return nil

	case user.EdgeFriendships:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of User", id, edge)}
			}
			i.Friendships.Add = append(i.Friendships.Add, value)
		}
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of User does not accept IDs", edge)}
}

func (i *UserPatch) petsIDs() []int {
	return i.Pets.Add

}

func (i *UserPatch) groupsIDs() []int {
	return i.Groups.Add

}

func (i *UserPatch) friendsIDs() []int {
	return i.Friends.Add

}

func (i *UserPatch) followersIDs() []int {
	return i.Followers.Add

}

func (i *UserPatch) followingIDs() []int {
	return i.Following.Add

}

func (i *UserPatch) friendshipsIDs() []int {
	return i.Friendships.Add

}

func (p *UserPatch) add(column string, value any) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of User does not support addition", column)}
}

func (p *UserPatch) appendValues(column string, value any) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of User does not support append", column)}
}

func (p *UserPatch) removeIDs(edge string, ids ...any) error {
	switch edge {

	case user.EdgePets:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q", id, edge)}
			}
			p.Pets.Remove = append(p.Pets.Remove, value)
			p.Pets.Add = slices.DeleteFunc(p.Pets.Add, func(id int) bool { return id == value })
		}
		return nil

	case user.EdgeGroups:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q", id, edge)}
			}
			p.Groups.Remove = append(p.Groups.Remove, value)
			p.Groups.Add = slices.DeleteFunc(p.Groups.Add, func(id int) bool { return id == value })
		}
		return nil

	case user.EdgeFriends:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q", id, edge)}
			}
			p.Friends.Remove = append(p.Friends.Remove, value)
			p.Friends.Add = slices.DeleteFunc(p.Friends.Add, func(id int) bool { return id == value })
		}
		return nil

	case user.EdgeFollowers:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q", id, edge)}
			}
			p.Followers.Remove = append(p.Followers.Remove, value)
			p.Followers.Add = slices.DeleteFunc(p.Followers.Add, func(id int) bool { return id == value })
		}
		return nil

	case user.EdgeFollowing:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q", id, edge)}
			}
			p.Following.Remove = append(p.Following.Remove, value)
			p.Following.Add = slices.DeleteFunc(p.Following.Add, func(id int) bool { return id == value })
		}
		return nil

	case user.EdgeFriendships:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q", id, edge)}
			}
			p.Friendships.Remove = append(p.Friendships.Remove, value)
			p.Friendships.Add = slices.DeleteFunc(p.Friendships.Add, func(id int) bool { return id == value })
		}
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of User does not support removal", edge)}
}

func (p *UserPatch) clearEdge(edge string) error {
	switch edge {

	case user.EdgePets:
		p.Pets.Clear = true
		return nil

	case user.EdgeGroups:
		p.Groups.Clear = true
		return nil

	case user.EdgeFriends:
		p.Friends.Clear = true
		return nil

	case user.EdgeFollowers:
		p.Followers.Clear = true
		return nil

	case user.EdgeFollowing:
		p.Following.Clear = true
		return nil

	case user.EdgeFriendships:
		p.Friendships.Clear = true
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of User cannot be cleared", edge)}
}

func (p *UserPatch) apply(other UserPatch) {

	if other.Name.IsSet() {
		p.Name = other.Name
		delete(p.expressions, user.FieldName)
	}

	p.Pets.Add = append(p.Pets.Add, other.Pets.Add...)
	p.Pets.Remove = append(p.Pets.Remove, other.Pets.Remove...)
	p.Pets.Clear = p.Pets.Clear || other.Pets.Clear

	p.Groups.Add = append(p.Groups.Add, other.Groups.Add...)
	p.Groups.Remove = append(p.Groups.Remove, other.Groups.Remove...)
	p.Groups.Clear = p.Groups.Clear || other.Groups.Clear

	p.Friends.Add = append(p.Friends.Add, other.Friends.Add...)
	p.Friends.Remove = append(p.Friends.Remove, other.Friends.Remove...)
	p.Friends.Clear = p.Friends.Clear || other.Friends.Clear

	p.Followers.Add = append(p.Followers.Add, other.Followers.Add...)
	p.Followers.Remove = append(p.Followers.Remove, other.Followers.Remove...)
	p.Followers.Clear = p.Followers.Clear || other.Followers.Clear

	p.Following.Add = append(p.Following.Add, other.Following.Add...)
	p.Following.Remove = append(p.Following.Remove, other.Following.Remove...)
	p.Following.Clear = p.Following.Clear || other.Following.Clear

	p.Friendships.Add = append(p.Friendships.Add, other.Friendships.Add...)
	p.Friendships.Remove = append(p.Friendships.Remove, other.Friendships.Remove...)
	p.Friendships.Clear = p.Friendships.Clear || other.Friendships.Clear

	for column, render := range other.expressions {
		p.setExpr(column, render)
	}
	for edge, clear := range other.clearedEdges {
		if clear {
			if p.clearedEdges == nil {
				p.clearedEdges = make(map[string]bool)
			}
			p.clearedEdges[edge] = true
		}
	}
}
