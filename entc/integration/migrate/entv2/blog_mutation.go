// Copyright 2019-2026 Facebook Inc.
// SPDX-License-Identifier: Apache-2.0

// Code generated by ent, DO NOT EDIT.

package entv2

import (
	"fmt"
	"slices"

	"github.com/neko-sc/ent"
	"github.com/neko-sc/ent/dialect/sql"
	"github.com/neko-sc/ent/entc/integration/migrate/entv2/blog"
)

// BlogInsert is the write representation of a Blog creation.
type BlogInsert struct {
	ID          ent.Option[int] `json:"id,omitzero"`
	Oid         int             `json:"oid"`
	AdminIDs    []int           `json:"admin_ids,omitzero"`
	expressions map[string]func(*sql.Builder)
}

// BlogPatch is the write representation of a Blog update.
type BlogPatch struct {
	Oid          ent.Option[int]        `json:"oid,omitzero"`
	OidAdd       ent.Option[int]        `json:"oid_add,omitzero"`
	Admins       ent.RelationPatch[int] `json:"admins,omitzero"`
	expressions  map[string]func(*sql.Builder)
	clearedEdges map[string]bool
}

func (i *BlogInsert) set(column string, value any) error {
	switch column {

	case blog.FieldID:
		v, ok := value.(int)
		if !ok {
			return &ValidationError{Name: column, err: fmt.Errorf("ent: unexpected type %T for field %q of Blog", value, column)}
		}
		i.ID = ent.Some(v)

		delete(i.expressions, column)
		return nil

	case blog.FieldOid:
		v, ok := value.(int)
		if !ok {
			return &ValidationError{Name: column, err: fmt.Errorf("ent: unexpected type %T for field %q of Blog", value, column)}
		}
		i.Oid = v

		delete(i.expressions, column)
		return nil

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Blog is not settable", column)}
}

func (i *BlogInsert) setNull(column string) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Blog is not nullable", column)}
}

func (i *BlogInsert) setExpr(column string, render func(*sql.Builder)) {
	if i.expressions == nil {
		i.expressions = make(map[string]func(*sql.Builder))
	}
	i.expressions[column] = render
}

func (i *BlogInsert) setEdge(edge string, id any) error {
	switch edge {

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Blog is not settable", edge)}
}

func (i *BlogInsert) addIDs(edge string, ids ...any) error {
	switch edge {

	case blog.EdgeAdmins:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of Blog", id, edge)}
			}
			i.AdminIDs = append(i.AdminIDs, value)
		}
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Blog does not accept IDs", edge)}
}

func (i *BlogInsert) adminsIDs() []int {
	return i.AdminIDs
}

func (i *BlogPatch) set(column string, value any) error {
	switch column {

	case blog.FieldOid:
		v, ok := value.(int)
		if !ok {
			return &ValidationError{Name: column, err: fmt.Errorf("ent: unexpected type %T for field %q of Blog", value, column)}
		}
		i.Oid = ent.Some(v)

		i.OidAdd = ent.Unset[int]()

		delete(i.expressions, column)
		return nil

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Blog is not settable", column)}
}

func (i *BlogPatch) setNull(column string) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Blog is not nullable", column)}
}

func (i *BlogPatch) setExpr(column string, render func(*sql.Builder)) {
	if i.expressions == nil {
		i.expressions = make(map[string]func(*sql.Builder))
	}
	i.expressions[column] = render
}

func (i *BlogPatch) setEdge(edge string, id any) error {
	switch edge {

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Blog is not settable", edge)}
}

func (i *BlogPatch) addIDs(edge string, ids ...any) error {
	switch edge {

	case blog.EdgeAdmins:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of Blog", id, edge)}
			}
			i.Admins.Add = append(i.Admins.Add, value)
		}
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Blog does not accept IDs", edge)}
}

func (i *BlogPatch) adminsIDs() []int {
	return i.Admins.Add

}

func (p *BlogPatch) add(column string, value any) error {
	switch column {

	case blog.FieldOid:
		delta, ok := value.(int)

		if !ok {
			return &ValidationError{Name: column, err: fmt.Errorf("ent: unexpected addition type %T for field %q", value, column)}
		}
		if current, exists := p.OidAdd.Get(); exists {
			delta += current
		}
		p.OidAdd = ent.Some(delta)
		return nil

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Blog does not support addition", column)}
}

func (p *BlogPatch) appendValues(column string, value any) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Blog does not support append", column)}
}

func (p *BlogPatch) removeIDs(edge string, ids ...any) error {
	switch edge {

	case blog.EdgeAdmins:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q", id, edge)}
			}
			p.Admins.Remove = append(p.Admins.Remove, value)
			p.Admins.Add = slices.DeleteFunc(p.Admins.Add, func(id int) bool { return id == value })
		}
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Blog does not support removal", edge)}
}

func (p *BlogPatch) clearEdge(edge string) error {
	switch edge {

	case blog.EdgeAdmins:
		p.Admins.Clear = true
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Blog cannot be cleared", edge)}
}

func (p *BlogPatch) apply(other BlogPatch) {

	if other.Oid.IsSet() {
		p.Oid = other.Oid
		delete(p.expressions, blog.FieldOid)
	}

	if value, ok := other.OidAdd.Get(); ok {
		if current, exists := p.OidAdd.Get(); exists {
			value += current
		}
		p.OidAdd = ent.Some(value)
	}

	p.Admins.Add = append(p.Admins.Add, other.Admins.Add...)
	p.Admins.Remove = append(p.Admins.Remove, other.Admins.Remove...)
	p.Admins.Clear = p.Admins.Clear || other.Admins.Clear

	for column, render := range other.expressions {
		p.setExpr(column, render)
	}
	for edge, clear := range other.clearedEdges {
		if clear {
			if p.clearedEdges == nil {
				p.clearedEdges = make(map[string]bool)
			}
			p.clearedEdges[edge] = true
		}
	}
}
