// Copyright 2019-2026 Facebook Inc.
// SPDX-License-Identifier: Apache-2.0

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"slices"

	"github.com/neko-sc/ent"
	"github.com/neko-sc/ent/dialect/sql"
	"github.com/neko-sc/ent/entc/integration/edgeschema/ent/process"
)

// ProcessInsert is the write representation of a Process creation.
type ProcessInsert struct {
	FileIDs         []int `json:"file_ids,omitzero"`
	AttachedFileIDs []int `json:"attached_file_ids,omitzero"`
	expressions     map[string]func(*sql.Builder)
}

// ProcessPatch is the write representation of a Process update.
type ProcessPatch struct {
	Files         ent.RelationPatch[int] `json:"files,omitzero"`
	AttachedFiles ent.RelationPatch[int] `json:"attached_files,omitzero"`
	expressions   map[string]func(*sql.Builder)
	clearedEdges  map[string]bool
}

func (i *ProcessInsert) set(column string, value any) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Process is not settable", column)}
}

func (i *ProcessInsert) setNull(column string) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Process is not nullable", column)}
}

func (i *ProcessInsert) setExpr(column string, render func(*sql.Builder)) {
	if i.expressions == nil {
		i.expressions = make(map[string]func(*sql.Builder))
	}
	i.expressions[column] = render
}

func (i *ProcessInsert) setEdge(edge string, id any) error {
	switch edge {

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Process is not settable", edge)}
}

func (i *ProcessInsert) addIDs(edge string, ids ...any) error {
	switch edge {

	case process.EdgeFiles:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of Process", id, edge)}
			}
			i.FileIDs = append(i.FileIDs, value)
		}
		return nil

	case process.EdgeAttachedFiles:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of Process", id, edge)}
			}
			i.AttachedFileIDs = append(i.AttachedFileIDs, value)
		}
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Process does not accept IDs", edge)}
}

func (i *ProcessInsert) filesIDs() []int {
	return i.FileIDs
}

func (i *ProcessInsert) attachedfilesIDs() []int {
	return i.AttachedFileIDs
}

func (i *ProcessPatch) set(column string, value any) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Process is not settable", column)}
}

func (i *ProcessPatch) setNull(column string) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Process is not nullable", column)}
}

func (i *ProcessPatch) setExpr(column string, render func(*sql.Builder)) {
	if i.expressions == nil {
		i.expressions = make(map[string]func(*sql.Builder))
	}
	i.expressions[column] = render
}

func (i *ProcessPatch) setEdge(edge string, id any) error {
	switch edge {

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Process is not settable", edge)}
}

func (i *ProcessPatch) addIDs(edge string, ids ...any) error {
	switch edge {

	case process.EdgeFiles:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of Process", id, edge)}
			}
			i.Files.Add = append(i.Files.Add, value)
		}
		return nil

	case process.EdgeAttachedFiles:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q of Process", id, edge)}
			}
			i.AttachedFiles.Add = append(i.AttachedFiles.Add, value)
		}
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Process does not accept IDs", edge)}
}

func (i *ProcessPatch) filesIDs() []int {
	return i.Files.Add

}

func (i *ProcessPatch) attachedfilesIDs() []int {
	return i.AttachedFiles.Add

}

func (p *ProcessPatch) add(column string, value any) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Process does not support addition", column)}
}

func (p *ProcessPatch) appendValues(column string, value any) error {
	switch column {

	}
	return &ValidationError{Name: column, err: fmt.Errorf("ent: field %q of Process does not support append", column)}
}

func (p *ProcessPatch) removeIDs(edge string, ids ...any) error {
	switch edge {

	case process.EdgeFiles:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q", id, edge)}
			}
			p.Files.Remove = append(p.Files.Remove, value)
			p.Files.Add = slices.DeleteFunc(p.Files.Add, func(id int) bool { return id == value })
		}
		return nil

	case process.EdgeAttachedFiles:
		for _, id := range ids {
			value, ok := id.(int)
			if !ok {
				return &ValidationError{Name: edge, err: fmt.Errorf("ent: unexpected ID type %T for edge %q", id, edge)}
			}
			p.AttachedFiles.Remove = append(p.AttachedFiles.Remove, value)
			p.AttachedFiles.Add = slices.DeleteFunc(p.AttachedFiles.Add, func(id int) bool { return id == value })
		}
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Process does not support removal", edge)}
}

func (p *ProcessPatch) clearEdge(edge string) error {
	switch edge {

	case process.EdgeFiles:
		p.Files.Clear = true
		return nil

	case process.EdgeAttachedFiles:
		p.AttachedFiles.Clear = true
		return nil

	}
	return &ValidationError{Name: edge, err: fmt.Errorf("ent: edge %q of Process cannot be cleared", edge)}
}

func (p *ProcessPatch) apply(other ProcessPatch) {

	p.Files.Add = append(p.Files.Add, other.Files.Add...)
	p.Files.Remove = append(p.Files.Remove, other.Files.Remove...)
	p.Files.Clear = p.Files.Clear || other.Files.Clear

	p.AttachedFiles.Add = append(p.AttachedFiles.Add, other.AttachedFiles.Add...)
	p.AttachedFiles.Remove = append(p.AttachedFiles.Remove, other.AttachedFiles.Remove...)
	p.AttachedFiles.Clear = p.AttachedFiles.Clear || other.AttachedFiles.Clear

	for column, render := range other.expressions {
		p.setExpr(column, render)
	}
	for edge, clear := range other.clearedEdges {
		if clear {
			if p.clearedEdges == nil {
				p.clearedEdges = make(map[string]bool)
			}
			p.clearedEdges[edge] = true
		}
	}
}
